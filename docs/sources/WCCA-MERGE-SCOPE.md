# WCCA merge scope

This branch contains only the operator-assisted WCCA/CCAP implementation and its supporting documentation/tests. It deliberately preserves current `main` behavior for other sources during merge reconciliation.
